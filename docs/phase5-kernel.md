# Phase 5: 系统层与可选专项（Month 7+）

> 到这一步你是"有测量纪律的延迟工程师"，可以按兴趣选方向。

## 前置条件

- [x] Phase 4 完成：TMA 归因能力已建立

## 5.1 内核旁路

### 路线选择

#### 路线 A: io_uring（广度入门）— ✅ 已实现模拟
- [x] 模拟 io_uring 提交/完成循环
- [x] 批量大小 A/B 测试 (1/4/16/64/256)
- [x] 批量提交开销分摊验证
- [ ] 真实 io_uring SQPOLL 集成（需 io_uring crate + kernel 5.1+）

#### 路线 B: AF_XDP — 框架就绪
- [x] 包级计时基础设施 (`net/packet_timer.rs`)
- [x] 模拟包接收器 (`net/raw_socket.rs`)
- [ ] 真实 XDP 程序加载和 UMEM 配置

#### 路线 C: DPDK — 待实现
- [ ] DPDK 绑定
- [ ] 轮询模式驱动

### io_uring 模拟结果

| 批量大小 | p50 (ns) | p99 (ns) | p99.9 (ns) |
|---------|----------|----------|------------|
| 1 | 60 | 80 | 80 |
| 4 | 120 | 130 | 150 |
| 16 | 370 | 380 | 961 |
| 64 | 1,372 | 1,452 | 10,463 |
| 256 | 5,331 | 11,055 | 11,055 |

### 包接收端到端延迟

| 阶段 | p50 (ns) | p99 (ns) | p99.9 (ns) |
|------|----------|----------|------------|
| 总 E2E | 1,422 | 12,295 | 31,343 |
| 订单簿 | 1,342 | 12,183 | 31,279 |

## 5.2 可选专项

### 5.2.1 WAL 延迟测量 — 框架就绪
- [ ] 顺序写优化
- [ ] fsync 延迟测量

### 5.2.2 时间同步 / PTP
- [ ] 硬件时间戳 vs 软件时间戳
- [ ] PTP 精度测量

### 5.2.3 迁移到 DB kernel
- [ ] order book → sort/merge 执行层
- [ ] SPSC → morsel-driven 并行
- [ ] cache 优化 → 列存 cache 布局
- [ ] TMA 归因 → 算子瓶颈分析
- [ ] Arena 分配 → 执行层内存管理

## 产出物

- [x] 网络层基础设施 (`net/`)
- [x] 包级计时 (`packet_timer.rs`)
- [x] io_uring 模拟基准 (`io_uring_bench.rs`)
- [x] 包接收器 (`raw_socket.rs`)
- [x] `net-bench` CLI 命令
- [ ] 真实 io_uring/AF_XDP 集成（需硬件/内核支持）
