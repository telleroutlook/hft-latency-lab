# Phase 3: 从"解析"扩到"系统"（Month 3–4）

> 从"会优化一个函数"到"会设计和归因一条延迟敏感数据通路"。

## 前置条件

- [ ] Phase 2 完成：itchy 优化有成果，测量纪律已内化

## 系统架构

```
[预加载字节流] → [解析器(你优化过的)] → [SPSC 环形队列] → [订单簿重建] → [策略回调桩]
```

## 3.1 零分配订单簿（灵魂约束）

### Arena + index 分配器 (`orderbook/arena.rs`)

**状态**: ✅ 框架已搭建

- [x] `OrderArena` — 开局一次性分配定长数组
- [x] `OrderNode` — index 代替指针 (0xFFFF_FFFF = null)
- [x] free_list 回收复用槽位
- [ ] 压力测试：百万级订单增删不 alloc
- [ ] 性能验证：p99.9 无 malloc 尾部刺尖

### 订单簿数据结构 (`orderbook/book.rs`)

**状态**: ✅ 框架已搭建

- [x] Arena-backed 双向链表（bid/ask 各一路）
- [x] add_order / cancel_order 基本操作
- [ ] **数据结构对比实验**（绝佳的可量化延迟实验）：
  - [ ] `BTreeMap` baseline（简单但 cache 不友好）
  - [ ] 价格阶梯数组（cache 友好但占内存）
  - [ ] 混合方案
  - [ ] 每种方案报 p99.9/p99.99 对比
- [ ] HashMap 索引加速 order_id 查找（替代线性扫描）

## 3.2 SPSC 无锁环形队列 (`pipeline/spsc.rs`)

**状态**: ✅ 框架已搭建

- [x] `SpscRing<T, CAP>` — 编译期确定容量
- [x] push/pop 基于 AtomicUsize + Acquire/Release
- [ ] **重点测尾延迟而非吞吐** — 新人最常翻车处
- [ ] 上无锁后必须重测尾延迟（无锁在竞争下常让 p99.9 变差）
- [ ] 跨核传递测试：解析在核 2，订单簿在核 3，测真实核间延迟

## 3.3 端到端尾延迟测量

- [ ] 每条消息打 TSC 时间戳
- [ ] 测**字节进入到策略回调触发的端到端 p99.9/p99.99**
- [ ] 找出整条链路里哪一段贡献了尾部（往往不是你以为的那段）
- [ ] 分段测量：解析延迟 / 队列传递延迟 / 订单簿更新延迟

## 3.4 策略回调桩

- [ ] 最简单的策略：best_bid/best_ask 变化时触发
- [ ] 回调内只记录时间戳，不做实际交易逻辑
- [ ] 端到端延迟 = 回调触发时间 - 字节到达时间

## 数据结构对比实验计划

| 方案 | 优点 | 缺点 | 预期 |
|------|------|------|------|
| BTreeMap<Price, Vec<Order>> | 简单，通用 | cache 不友好，堆分配 | baseline |
| 价格阶梯数组 | cache 友好，O(1) | 占内存，固定范围 | p99.9 ↓ |
| Arena linked list | 零分配，紧凑 | 遍历不连续 | 待测 |
| 混合：Arena + sorted array of levels | 兼顾两者 | 实现复杂 | 最优？ |

## 通过标准

- [ ] 零分配订单簿：热路径无任何 heap allocation
- [ ] 端到端尾延迟分布报告
- [ ] 瓶颈归因：知道尾延迟来自哪一段
- [ ] 数据结构对比实验 + 诚实结算
